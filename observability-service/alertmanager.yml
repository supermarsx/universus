global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h
receivers:
  - name: 'default'
    email_configs:
      - to: 'your-team@example.com'
        send_resolved: true
# Add Slack, PagerDuty, etc. as needed
