[package]
name = "game-universe"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
platform-common = { path = "../platform-common" }
serde = { workspace = true, features = ["derive"] }
serde_json = "1"
