Files added for status page:
- `status.html` public page
- `status.ts` frontend logic that fetches `/status` and shows admin controls when admin token present

Serve `status.html` from the frontend static files (e.g., add link to navigation) and build the TypeScript to `/js/status.js` as per project build setup.